<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatPageActivity" />
        <activity android:name=".CreatePageActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".JobApplyPageActivity" />
        <activity android:name=".JobDetailPageActivity" />
        <activity android:name=".MapPageActivity" />
        <activity android:name=".MessagesPageActivity" />
        <activity android:name=".NotificationPageActivity" />
        <activity android:name=".ProfilePageActivity" />
        <activity android:name=".SearchPageActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".VerificationPageActivity" />
        <activity android:name=".VerificationStatusActivity" />
    </application>

</manifest> 